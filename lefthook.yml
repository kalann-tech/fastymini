pre-commit:
  parallel: true
  commands:
    lint-schedule-service:
      run: cd services/schedule-service && yarn lint
      stage_fixed: true

commit-msg:
  commands:
    lint-commit-msg:
      run: yarn commitlint --edit {1}
